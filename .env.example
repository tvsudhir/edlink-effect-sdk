# Edlink Effect SDK - Environment Configuration
# Using Effect-TS Config System for Type-Safe Configuration

# ==========================================
# REQUIRED: Edlink API Credentials
# ==========================================
# Obtain these from your Edlink Developer Dashboard
# https://developer.ed.link

# Your Edlink Client ID
EDLINK_CLIENT_ID=your_client_id_here

# Your Edlink Client Secret (used as Bearer token)
# ⚠️  NEVER commit .env.local files! Always use .env.example as template
# The Config system automatically redacts secrets in logs
EDLINK_CLIENT_SECRET=your_client_secret_here

# ==========================================
# OPTIONAL: API Configuration
# ==========================================

# Edlink API Base URL
# Default: https://ed.link/api
# Override only if using a custom endpoint
# EDLINK_API_BASE_URL=https://ed.link/api

# Default maximum pages to fetch
# Default: 3
# Used when no pagination config is specified in API calls
# EDLINK_DEFAULT_MAX_PAGES=3

# ==========================================
# OPTIONAL: Example Selector
# ==========================================
# Choose which example to run with `pnpm dev`
# Default: 1

# EXAMPLE=1  # Fetch default 3 pages
# EXAMPLE=2  # Fetch all available data
# EXAMPLE=3  # Fetch with max record limit
# EXAMPLE=4  # Process sequentially (memory-efficient)
# EXAMPLE=5  # Take first N items
# EXAMPLE=6  # Fetch people data
# EXAMPLE=7  # Merge multiple streams
# EXAMPLE=8  # Compare pagination strategies

# ==========================================
# NOTES ON ENVIRONMENT-SPECIFIC CONFIGS
# ==========================================
# This SDK uses Effect's Config system which supports multiple config sources:
#
# 1. .env.local (local development)
# 2. .env.staging (staging environment)
# 3. .env.production (production environment)
#
# Example: to use staging config, create .env.staging with your staging credentials
# The Config system will prioritize .env.local over .env files
